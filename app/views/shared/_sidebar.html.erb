<div class="column" id="sidebar">
  <aside class="menu" id="menu">
    <p class="menu-label">
      部位一覧
    </p>
    <ul class="menu-list" id="sidebar-list">
      <% @categories.each do |category| %>
        <li>
          <%= link_to "#{category.name}", category_path(category), class: "sidebar-parts"%>
        </li>
      <% end %>
    </ul>
  </aside>
  <p class="hover">部位一覧</p>
</div>